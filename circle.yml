
machine:
  timezone: UTC
  services:
    - docker

dependencies:
  pre:
    - docker version
    - docker info
  post:
    - docker build -t builder .

test:
  override:
    - docker run --rm --privileged -v $(pwd):/data builder /build.sh
    - mv rootfs.tar.gz $CIRCLE_ARTIFACTS/
